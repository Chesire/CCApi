<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Development -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSX</pattern>
                    </timestamp>
                    <version/>
                    <logLevel/>
                    <message/>
                    <mdc/>
                    <loggerName/>
                    <threadName/>
                    <stackTrace/>
                </providers>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.chesire.capi" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <logger name="org.springframework.web" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

    <!-- Production -->
    <springProfile name="prod">
        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <http class="com.github.loki4j.logback.JavaHttpSender">
                <url>${GRAFANA_LOKI_URL}</url>
                <auth>
                    <username>${GRAFANA_LOKI_USERNAME}</username>
                    <password>${GRAFANA_LOKI_PASSWORD}</password>
                </auth>
            </http>
            <format>
                <label>
                    <pattern>application=capi,host=${HOSTNAME:-localhost},level=%level,logger=%logger{20}</pattern>
                </label>
                <message>
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
                </message>
            </format>
        </appender>

        <root level="WARN">
            <appender-ref ref="LOKI"/>
        </root>
        <logger name="com.chesire.capi" level="INFO" additivity="false">
            <appender-ref ref="LOKI"/>
        </logger>
        <logger name="org.springframework.web" level="WARN" additivity="false">
            <appender-ref ref="LOKI"/>
        </logger>
        <logger name="org.springframework.security" level="WARN" additivity="false">
            <appender-ref ref="LOKI"/>
        </logger>
    </springProfile>
</configuration>
